import {describe, it} from 'mocha';

describe('Graph API', () => {
  describe('graph.get', () => {
    it('is missing tests');
  });
  describe('graph.create', () => {
    it('is missing tests');
  });
  describe('graph.drop', () => {
    it('is missing tests');
  });
});

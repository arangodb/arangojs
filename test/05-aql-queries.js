'use strict';
import 'core-js/shim';
import {describe, it} from 'mocha';

describe('AQL queries', () => {
  describe('database.query', () => {
    it('is missing tests');
  });
  describe('aqlQuery', () => {
    it('is missing tests');
  });
});

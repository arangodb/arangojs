'use strict';
import 'core-js/shim';
import {describe, it} from 'mocha';

describe('EdgeCollection API', () => {
  describe('edgeCollection.edge', () => {
    it('is missing tests');
  });
  describe('edgeCollection.edges', () => {
    it('is missing tests');
  });
  describe('edgeCollection.inEdges', () => {
    it('is missing tests');
  });
  describe('edgeCollection.outEdges', () => {
    it('is missing tests');
  });
  describe('edgeCollection.traversal', () => {
    it('is missing tests');
  });
  describe('edgeCollection.save', () => {
    it('is missing tests');
  });
});

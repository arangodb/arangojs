import {describe, it} from 'mocha';

describe('GraphEdgeCollection API', () => {
  describe('graphEdgeCollection.edge', () => {
    it('is missing tests');
  });
  describe('graphEdgeCollection.save', () => {
    it('is missing tests');
  });
  describe('graphEdgeCollection.edges', () => {
    it('is missing tests');
  });
  describe('graphEdgeCollection.inEdges', () => {
    it('is missing tests');
  });
  describe('graphEdgeCollection.outEdges', () => {
    it('is missing tests');
  });
  describe('graphEdgeCollection.traversal', () => {
    it('is missing tests');
  });
});
